// modules/payments/stripe.js
// Placeholder para lógica avanzada de Stripe (Checkout, Webhooks, etc.)

// Aquí en el futuro podrás tener funciones como:
// - createCheckoutSessionForPlan(userId, plan, metadata)
// - handleStripeWebhookEvent(event)
// - mapStripeSubscriptionToBotSubscription(...)

module.exports = {
  // Por ahora vacío, listo para expandir
};
